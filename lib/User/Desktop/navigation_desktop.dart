import 'package:demoproject/Constants/const_widget.dart';
import 'package:flutter/material.dart';

class UserNavigationDesktop extends StatelessWidget {
  const UserNavigationDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(color: AppConst.tealColor,),);
  }
}
